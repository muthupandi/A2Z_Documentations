
1) what is sleep function() ? 
2) getopt and 
3) How do connect the database using php ? 
 
6) Tell some array function in php?
7) Tell some string function in php? 



Javascript:

1) tell some array functions in javascript ? 
2) tell some string functions in javascript ? 

Jquery:

1) What is the use of find() function?
2) How do check if class is exist in the jquery ? 

Magento 2:

1) Data migration in php? 
2) What is the use config.php file? 
3) What is the use of env.php file?
4) what is the use of parameter.php file? 
5) what are the files available in module ? 
6) How do create table for custom table? 
7) what are the files are available in 
8) How do override the controller in magento 2?
9) what is plugin in magento 2?
10) How to optimize the magento 2? 
11) Difference between cache:clean and cache:flush? 
12) Difference between varnish cache and redis cache?


Mysql:

1) Explain mysql store engine ?
2) what is mysql query cache?
3) What is left join ?
4) Difference between Orderby and groupby? 






 

